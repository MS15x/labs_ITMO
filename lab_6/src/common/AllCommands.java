package lab_6.common;

/**
 * Список всех доступных комманд
 */
public enum AllCommands {
    add,
    add_if_max,
    add_if_min,
    clear,
    count_greater_then_type,
    count_less_then_price,
    execute_script,
    exit,
    help,
    inf_by_id,
    info,
    remove_by_id,
    remove_lower,
    show,
    service,
    update
}
